from setuptools import setup, find_packages

setup(
    name="figure_area",
    version="1.0.0",
    description="Library for calculating the area of geometric figures.",
    author="Kseniya Gurevich",
    packages=find_packages(),
    install_requires=[],
)